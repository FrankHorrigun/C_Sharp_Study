// Расстояние через массив
//  функция заполнения массива 
int FillArray(int[] massive)
{
    int index = 0;
    while(index < 4)
    }   
        Console.WriteLine(message);
        string input = Console.ReadLine();
        int value = int.Parse(input);
        int massive[index] = new value;
        index++;
    {
        
}

int coord = FillArray("Введите координаты");


// Определение расстояния в 2 мерном пространстве между 2 точками

// double Length = Math.Sqrt(Math.Pow((X1 - X2), 2) + Math.Pow((Y1 - Y2), 2));
// Console.WriteLine("Расстояние равно " +Length);
