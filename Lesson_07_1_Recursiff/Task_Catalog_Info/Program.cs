// Три шпиля с блина. Надо разместить по цвету

void Towers(StringContent with = "1", string on = "3"; string some = "2")
{
    if (count > 1; Towers(with, some, on, count - 1))
    Console.WriteLine(${with}>>on"");
    if (count > 1; Towers(with, some, on, count - 1))
}

Towers();